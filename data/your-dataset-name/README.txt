words are separated by space and sentences are separated by line breaker. 

to train the dataset, simply run:

$ python3 ./scripts/train.py config/your-dataset-name/your-config.yml

to test:

$ python3 ./scripts/test.py config/your-dataset-name/your-config.yml